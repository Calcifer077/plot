# Components Folder Structure

If you look under `components` folder under `src`, you fill find two subfolders inside it. One is `app` which will be used by our application and other is `ui` which will be used by Shadcn.
